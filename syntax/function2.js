console.log(Math.round(1.6)); // 반올림 : 2
console.log(Math.round(1.4)); // 반올림 : 1

function sum(first, second) { // 매개변수, parameter
    console.log(first + second);
}
SubmitEvent(2, 4); // 인자, argument